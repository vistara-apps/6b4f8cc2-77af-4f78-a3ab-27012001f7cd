'use client';

// Simplified providers for now - wallet integration can be added later
export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {children}
    </div>
  );
}
